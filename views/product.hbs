


    <h1>Product: {{title}}</h1>

    <h2>Price: {{price}}€</h2>

   

        {{#if imgFile}}
        <img src="/images/{{imgFile}}" alt="{{title}}" />
    {{else}}
        <img src="https://via.placeholder.com/600x400" alt="{{title}}" />
    {{/if}}

        <div>
        <h5>This product is available in the following stores:</h5>
        {{stores}}
    </div>

      {{#if stores}}
            <h5>This product is available in the following stores:</h5>

            <ul>
                {{#each stores}}
                <li>
                    {{this}}
                    <a href="#">More Details</a>
                </li>
                {{/each}}
            </ul>
        {{else}}
            <h5>Product currently not available.</h5>
        {{/if}}

